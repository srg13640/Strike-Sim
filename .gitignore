# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Editor / OS noise
*.swp
*~
Thumbs.db

# Claude Code local (machine-specific permissions/cache); keep shared config only
.claude/settings.local.json

# Internal design doc kept out of the shared repo (concept by Pat Beaudry); stays local
/Janus_Implementation_Plan.md

# Local preview server state (keep launch.json — it's shared config)
# (nothing to ignore here yet)

# Local map tiles are environment-specific; bundle deliberately, not by accident.
# Uncomment if tiles should stay out of version control:
# /tiles/

# stray npm package dir (the vendored single file lives in vendor/)
milsymbol/

# --- added v0.1.1 (TRIP health baseline) ---

# Nested git repos — never track as gitlinks (hosted layer parked; ARCHI §14)
site/
site-preview/

# Agent workspaces, idea dumps, third-party material — reference, not source
_CODEX Ideas/
_GEMINI Ideas/
_codex_review/
_design_explorations/
_fable/
_stark/
Matt Olson's project/

# Local run artifacts
loop-runs/
sandbox/

# iCloud-pinned git lock copies renamed by agents (inert)
*.lock.stale*
*.lock.s[0-9]*
