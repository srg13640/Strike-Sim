# Health Baseline Implementation Plan

UNCLASSIFIED // NOTIONAL RESEARCH TOOL

## Overview

First TRIP cycle: restore the proof suite to fully green and record a verified repo baseline. The guided-tutorial commit (`6d82079`) extended `director.js` with an `op.tutorial` branch, which broke 2 of 48 static contracts in `tools/online-layer-proof.js` (46/48). This plan updates those contracts, pins the tutorial's share/replay semantics as new contracts so they can't silently drift, runs the 200-match balance gate, and writes the baseline to `docs/6-memo/` so future sessions trust a recorded state instead of re-auditing the repo.

## Problem Statement

- `online-layer-proof.js` exits 1: two static regexes (proof lines ~100–101) predate the tutorial branch at `director.js:617` (`op.startModel`) and `:624` (seed selection).
- The tutorial's integrity rules (fixed seed 204002, neutral player model, no challenge intake, no challenge-link export) exist in code but are unpinned — any future tutorial edit could break replay integrity invisibly.
- The balance gate has not been run since the contested-logistics merge (`3c320b0`/`91b4253`); current Blue win rate is unknown.

## Solution Architecture

Test-harness and documentation change only — zero engine, UI, or data changes. Extend the existing static-contract section of `online-layer-proof.js` (same `check(name, regex)` style), then measure and record.

## Implementation Details

### 1. tools/online-layer-proof.js

**File**: `tools/online-layer-proof.js` (static "director wiring" section, ~lines 97–107)

**Current state**: 48 contracts; 2 fail because their regexes assert the pre-tutorial source.

**Modifications**:

- Update `challenge seed only ever comes from op.challenge` → assert seed comes only from `op.challenge.seed` or the fixed tutorial constant `204002`, never from player/user state (matches `director.js:624`).
- Update `pre-match model stashed for replay payloads (I-4)` → assert `op.startModel = (op.challenge || op.tutorial) ? null : readPlayerModel()` (matches `director.js:617`).
- Add contract `tutorial never consumes a pending challenge` → asserts the `op.challenge` assignment nulls under tutorial before `consumePending` (matches `director.js:571`).
- Add contract `tutorial world is pinned to the fixed seed` → asserts the `204002` constant appears only in the tutorial seed branch.
- Add contract `AAR challenge link is tutorial-gated` → asserts the `!op.tutorial &&` guard on the `challenge-link` button (matches `director.js:2143`).

Expected result: **51/51** contracts, exit 0. E2E contracts untouched.

### 2. docs/6-memo/2026-07-19-health-baseline.md

**File**: new memo recording the verified baseline: syntax gate result (55 files), per-proof results with contract counts, validate-scenarios, balance-gate Blue win rate with seed bases used, environment notes, and the statement that main@<commit> is the trusted baseline.

### 3. Balance gate run (measurement, not code)

Chunked in the sandbox: `node tools/wargame-loop-eval.js --matches 20 --seed-base N` for 10 disjoint seed bases (200 matches total), aggregated Blue win rate checked against the 0.45–0.55 band. **If out of band: record the finding in the memo — rebalancing is explicitly out of scope and becomes its own TRIP cycle.**

## Technical Considerations

- **Pattern Usage**: Extend the existing proof's `check()`/static-grep style; do not create a parallel proof file (TESTING.md rule).
- **Determinism**: No engine paths touched; gate/eval runs are read-only measurements. Proof must remain source-order tolerant (regexes anchored to semantics, not incidental whitespace).
- **Resolver Discipline**: Untouched.
- **Offline-Complete**: Untouched; proof continues to enforce no-sockets/no-`Math.random` checks.
- **Proof Impact**: This IS the proof change; full suite re-run afterward proves no collateral drift.
- **Edge Cases**: Regexes must tolerate future minification-safe edits (quote style, spacing) without going stale again — prefer matching identifiers and constants over exact whitespace.

## Design-Spine Compliance

- [x] No engine fork — no adjudication code touched; `game.js` untouched.
- [x] No new top-level mode — no UI/mode changes; the tutorial already enters through the loop.
- [x] Determinism preserved — test-only change; gate/eval are measurements of existing behavior.
- [x] Offline-complete kept — no network surface touched; proof keeps enforcing it.
- [x] No prediction theater — baseline memo reports measured rates with seed provenance, no point claims.

## Files to Modify/Create

1. `tools/online-layer-proof.js` (modify) — 2 contracts updated, 3 added (48 → 51)
2. `docs/6-memo/2026-07-19-health-baseline.md` (new) — verified baseline record

## Test Impact

- The changed file is itself a test; acceptance = `node tools/online-layer-proof.js` exits 0 at 51/51.
- Full proof suite + `validate-scenarios.js` + syntax gate re-run to confirm no collateral reds (expected: all green as measured 2026-07-19).
- Balance gate: 200 chunked matches; result recorded either way; out-of-band ⇒ finding only.
- No new app logic ⇒ no new tests beyond the added contracts.

## To-dos

- [x] Update stale contract: challenge-seed check accepts the tutorial constant branch
- [x] Update stale contract: I-4 startModel check accepts the tutorial branch
- [x] Add contract: tutorial never consumes a pending challenge payload
- [x] Add contract: tutorial world pinned to fixed seed 204002 (single-sourced + honestly displayed)
- [x] Add contract: AAR challenge link is tutorial-gated
- [x] Verify `online-layer-proof.js` 51/51 + full proof suite + syntax gate green
- [x] Run balance gate — executed as 30 × 7 matches (210 total, disjoint bases 42…2942; sandbox slice limit forced smaller chunks than planned): Blue 0.457, PASS
- [x] Write `docs/6-memo/2026-07-19-health-baseline.md` (gate in band; findings F1–F3 recorded)
